filme1 = ["O Mascara", 1994, 7.0, True]

lista_filmes = ["O Poderoso Chefão", "Vingadores - Ultimato", "A Origem",
                 "Batman O Cavaleiro Das Trevas", "Ela"]

# Buscar os dois primeros itens na lsita
print(lista_filmes[:2])  # vejamos, eu decidi invocar em uma lista os dois primeiros indices, na lista os indices são separados por ","
                         # os dois primeiros indices, na lista os indices são separados por ","

print(lista_filmes[-1:]) # nesse caso eu quero o ultimo item da lista 

print(lista_filmes[:3]) # aqui eu posso ultilizar também para buscar itens na lista até uma determinada posição
                        # no caso até o 3° indice.

print(lista_filmes[1:3]) # nesse caso eu quero que apareça os itens na lista apartir do indice 1, e que vá até o indice 3
